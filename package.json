{
  "name": "bitacora",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "Repo raíz. La app en producción vive en web/ (React). Ver README.md.",
  "scripts": {
    "dev": "cd web && npm run dev",
    "build": "(cd web && npm run build) && node --check netlify/functions/bitacora.mjs",
    "legacy:dev": "node legacy-vanilla-app/server.js"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0"
  }
}
