# Copia este archivo a .env.local (Vite lo carga automático, y .env.local
# ya está en .gitignore) y complétalo con los valores reales.
#
# Estas variables se compilan DENTRO del bundle del navegador (Vite expone
# todo lo que empieza con VITE_). La anon key es pública por diseño — no es
# un secreto — así que esto es seguro.

VITE_SUPABASE_URL=https://rjmftwujvusprlbxtbbg.supabase.co
VITE_SUPABASE_ANON_KEY=tu_anon_key_publica

# En Netlify: Site settings > Environment variables, con estos mismos nombres
# (deben empezar con VITE_ para que Vite los incluya en el build).
